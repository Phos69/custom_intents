#intent_script:

      
      
      

######### TEST
  test: 
    speech: 
      text: "{{name}}  {{ states | selectattr('name', 'in', name) | map(attribute='attributes.alias') | list | first | default }}"
      
    # # action:
      # # service: "light.toggle"
      # # data:
        # # entity_id: "{{ states.light[name].state}}"
     # #   pippoll tapparelle
    # # speech:
         
      # # text: "{{name}} Test OK! {{ states.cover | selectattr('name', 'in', name) | selectattr('entity_id', 'in', area_entities(area)) | map(attribute='entity_id') | list | first | default }}"
      
  StopCovertest:
    speech:
      text: "{{device_class}} {{cover_classes}}"