language: "it"
intents:
  test:
    data:
      - sentences:
          - test {name} #in {area}
        # requires_context:
          # domain: cover
  StopCovertest:
    data:
      - sentences:
          - "test2 {cover_classes:device_class} [<in> | <of>] {area}" #apri tutte le tapparelle in salotto
    #    response: "cover_device_class_area"

# responses:
  # intents:
    # StopCovertest:
      # default: Fermate tutte le {{cover_class}} 
